import { ConfirmBookingData } from './confirm-booking-data';

describe('ConfirmBookingData', () => {
  it('should create an instance', () => {
    expect(new ConfirmBookingData()).toBeTruthy();
  });
});
