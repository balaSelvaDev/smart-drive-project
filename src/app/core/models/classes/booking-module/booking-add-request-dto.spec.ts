import { BookingAddRequestDTO } from './booking-add-request-dto';

describe('BookingAddRequestDTO', () => {
  it('should create an instance', () => {
    expect(new BookingAddRequestDTO()).toBeTruthy();
  });
});
